# 后续功能记录

## 无损双语活动存档转换：保留地区／路途随机事件牌堆

状态：未实现。当前版本使用安全兼容策略：英文与中文活动存档互转时，重置已保存的地区／路途随机事件牌堆，让目标语言包重新生成它们。

### 为什么现在不直接转换

这些牌堆不是单纯的事件标题列表。保存内容混合了：

- Forth 词标识（读取时会被执行或解析）；
- 玩家可见的事件文本；
- 富文本控制字节；
- 随机牌堆剩余顺序。

中文补丁会本地化其中一部分词标识和控制文本。把英文原始字节带到中文，或反过来带到英文，会使 `$load-savefile` 在区域／路途状态处停止执行，导致位于后方的天数、游戏模式等字段没有读入。

### 当前策略的影响

会保留角色、物资、车辆、剩余天数、游戏模式，以及当前主流程 `road-stack`。目标语言会重新生成未来事件牌堆，因此已见过的少量随机事件可能再次出现；这不是重开存档。

### 未来的实现条件

要实现真正无损的牌堆迁移，需要：

1. 一份与支持版本严格对应的英文原版 `deathforth` 完整源码树，以及中文补丁源码树；
2. 从两份源码按事件定义、构造上下文生成双向“原始 Forth 项字节序列”映射，而不是仅映射显示文字；
3. 对每个存档内牌堆项逐项转换，并保持剩余顺序不变；
4. 遇到未收录条目时阻止写入，绝不能猜测替换；
5. 使用多组英文／中文实机存档验证：牌堆可读、天数与模式保持、事件不会因转换而重复或丢失。

在满足这些条件前，继续使用当前重置牌堆的兼容模式。
